info:
  name: patchTasks
  type: http
  seq: 1

http:
  method: PATCH
  url: http://localhost:3000/tasks/update/1
  body:
    type: json
    data: '{"id":"1", "title":"create update api","description":"hello everyine"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
