info:
  name: createTasks
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/tasks/create
  body:
    type: json
    data: |-
      {
        "title":"create delete",
        "description":"update api"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
